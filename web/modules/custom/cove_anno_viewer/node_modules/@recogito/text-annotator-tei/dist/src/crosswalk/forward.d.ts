import { TEIAnnotation, TEIAnnotationTarget, TEIRangeSelector } from '../TEIAnnotation';
import { TextAnnotation, TextAnnotationTarget, TextSelector } from '@recogito/text-annotator';
/**
 * Using the DOM Range from a (revived!) TextSelector, this function computes
 * the TEIRangeSelector corresponding to that range.
 */
export declare const textToTEISelector: (selector: TextSelector) => TEIRangeSelector;
export declare const reviveTarget: (t: TextAnnotationTarget, container: HTMLElement) => TextAnnotationTarget;
export declare const textToTEITarget: (container: HTMLElement) => (t: TextAnnotationTarget) => TEIAnnotationTarget;
export declare const textToTEIAnnotation: (container: HTMLElement) => (a: TextAnnotation) => TEIAnnotation;
